# Epic 2 Retrospective — One-Shot Upload

**Date:** 2026-03-14
**Facilitator:** Bob (Scrum Master)
**Participants:** Grochonnou (Project Lead), Alice (Product Owner), Charlie (Senior Dev), Dana (QA Engineer)

## Epic Summary

- **Epic:** 2 — One-Shot Upload
- **Stories Completed:** 2/2 (100%)
- **Total Tests:** 53 (43 from Epic 1 + 6 from Story 2.1 + 4 from Story 2.2)
- **Technical Debt:** 0
- **Agent Used:** claude-sonnet-4-6 (dev), claude-opus-4-6 (review)

### Stories Delivered

| Story | Title | Review Findings |
|-------|-------|-----------------|
| 2.1 | Core Effect Implementation | 0 HIGH, 1 MEDIUM fixed (AC text corrected: `Effect.race` → `Effect.raceFirst`), 1 finding reclassified as non-issue |
| 2.2 | Dual API Entry Point | 0 HIGH, 0 MEDIUM, 1 LOW fixed (merged duplicate import) |

## Previous Retro Follow-Through (Epic 1)

| # | Action Item | Status | Evidence |
|---|-------------|--------|---------|
| 1 | Enforce surgical test assertions (extract `Cause`, validate error type + content) | ✅ Completed | Story 2.1: 6 tests with `Cause.failureOption` + `_tag` assertions. Story 2.2: abort test with `Effect.tryPromise` + `Cause.failureOption` |
| 2 | Update `project-context.md` — remove `isolatedDeclarations: true` | ✅ Completed | Story 2.1 Task 0 — executed before source code |

**Result:** 2/2 action items completed. Improvement cycle working.

## Successes

- **All Epic 1 retro action items executed**: Surgical test assertions applied from the start — no review reminders needed. `project-context.md` corrected as Task 0 before touching code.
- **Single-run pattern correctly implemented**: Story 2.2 avoided the double-execution trap (user callback invoked twice) using `runPromiseExit` + `Cause.squash` for shared `collected` Promise.
- **Dev agent self-corrected architecture error**: Story 2.1 discovered `Effect.raceFirst` (not `Effect.race`) is required for abort interop — corrected during dev, validated in review.
- **Sonnet + Opus cycle continues to deliver**: Both stories had clean or near-clean reviews. Story 2.2 review found only 1 LOW cosmetic issue.
- **Zero technical debt**: Epic delivered clean — no shortcuts, no deferred work.

## Challenges

- **Architecture doc partially incorrect**: `architecture.md` still prescribes `Effect.race` for AbortSignal interop pattern. `project-context.md` was corrected, but the architecture source was not updated. Risk: dev agents reading architecture first may reproduce the error.
- **Project Lead did not read generated code**: The Sonnet (dev) + Opus (review) pipeline produced correct code, but the human did not verify the implementation directly. The pipeline compensates, but this creates a fragile dependency on AI review quality.

## Key Insights

1. **`FiberFailure` wrapping pattern is reusable**: `Effect.runPromiseExit` + `Cause.squash` ensures Promise API rejects with raw typed errors (not `FiberFailure` wrappers). Must be applied in all future Dual API wrappers (Story 3.3, etc.).
2. **Architecture doc corrections must happen at the source**: Fixing only `project-context.md` creates divergence. The architecture doc is the primary reference for story spec authors.
3. **Detailed story specs compensate for lack of human code reading** — but this is fragile and depends on review quality.
4. **Previous retro action items were fully executed** — the improvement cycle works when items are embedded in story specs (Task 0 pattern).

## Action Items

| # | Action | Owner | Success Criteria |
|---|--------|-------|------------------|
| 1 | Correct `architecture.md`: replace `Effect.race` with `Effect.raceFirst` in AbortSignal Interop Pattern section | Dev (story spec) | All mentions of abort interop in architecture.md say `Effect.raceFirst` |
| 2 | Document `FiberFailure` wrapping pattern (`runPromiseExit` + `Cause.squash`) in Epic 3 story specs | SM (story specs) | Story 3.3 spec includes the pattern with rationale |

## Team Agreements

- Sonnet (dev) + Opus (review) cycle continues for all future epics
- Story specs remain the #1 quality lever
- Architecture doc corrections must be made at the source — not only in `project-context.md`
- Discoveries that invalidate architecture assumptions are corrected in `architecture.md` before the next epic starts

## Next Epic Preview

**Epic 3: Multipart Upload** — 4 stories (3.1 Chunk Stream, 3.2 Core Effect, 3.3 Dual API, 3.4 Circuit Breaker)

**Dependencies on Epic 2:** All satisfied — `uploadOnceEffect` pattern (normalizeCallback + Effect.raceFirst + fromAbortSignal), Dual API wrapper pattern (runPromiseExit + Cause.squash), `UploadEvent` type (minimal, additive extension)

**New capabilities needed:**
- `chunkStream` — WHATWG TransformStream → Effect Stream, backpressure
- `Effect.Semaphore` for parallel concurrency control
- `Effect.Schedule` injectable for retry
- `Effect.Ref<Progress>` for `getProgress()` pull-mode
- Circuit Breaker state machine (Closed → Open → HalfOpen)

**Preparation needed:**
- Action Item #1 (correct architecture.md) — must complete before Story 3.2 spec
- `UploadEvent` type will be extended additively (same pattern as Story 2.1)

**Blockers:** None after Action Item #1 is completed.

## Readiness Assessment

- Testing & Quality: ✅ 53 tests, 0 failures
- Technical Health: ✅ Zero debt, clean build
- Stakeholder Acceptance: ✅
- Unresolved Blockers: ✅ None (after Action Item #1)

**Verdict:** Epic 2 complete. Clear to proceed to Epic 3 after correcting architecture.md.
